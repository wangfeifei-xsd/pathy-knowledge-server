# 浏览器经 digital.minivision.cn → agent-plant /agent/wiki-external → pathy-knowledge-server
# 上传 APK/大视频时若返回 HTTP 413，多为 Tengine/Nginx 在应用前拦截，需调大请求体上限。

# 方式一：全站（按你们现有 server 块合并）
# client_max_body_size 200m;

# 方式二：仅 Wiki 代理路径（推荐）
# location /agent/wiki-external/ {
#     client_max_body_size 200m;
#     proxy_read_timeout 600s;
#     proxy_send_timeout 600s;
#     # ... 其余 proxy_pass 到 agent-plant:5656 的配置
# }
